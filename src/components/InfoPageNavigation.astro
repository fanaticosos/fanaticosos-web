---
import SiteNavigation from "./SiteNavigation.astro";

interface Props {
  locale?: "es" | "en";
  alternatePath: string;
}

const { locale = "es", alternatePath } = Astro.props;
---
<SiteNavigation locale={locale} alternatePath={alternatePath} />
