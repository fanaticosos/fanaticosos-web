{
  "name": "fanaticosos-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "npm run validate:content && astro check && astro build && npm run validate:build",
    "validate:content": "node scripts/validate-content.mjs",
    "test:content-validation": "node scripts/test-content-validation.mjs",
    "test:article-routes": "node scripts/test-article-routes.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "astro": "7.1.4",
    "js-yaml": "4.3.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
